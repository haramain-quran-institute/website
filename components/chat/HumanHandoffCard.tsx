import Link from "next/link";
import { ArrowUpRight, Clock3, Headphones, ShieldCheck } from "lucide-react";

export default function HumanHandoffCard({ visitorContext }: { visitorContext: Record<string, string> }) {
  const details = Object.entries(visitorContext);
  return <article className="mt-4 rounded-[14px] border border-[#0D463E]/12 bg-[#071F1B] p-5 text-white"><div className="flex items-start gap-3"><div className="grid size-11 shrink-0 place-items-center rounded-full bg-[#D0A86C] text-[#071F1B]"><Headphones className="size-5" /></div><div><p className="text-[10px] font-bold uppercase tracking-[.17em] text-[#D0A86C]">Human support handoff</p><h4 className="mt-2 font-heading text-xl">Connecting you with Haramain Team</h4></div></div><div className="mt-5 rounded-[9px] border border-white/10 bg-white/[.05] p-4"><p className="flex items-center gap-2 text-xs font-semibold text-white/70"><Clock3 className="size-3.5 text-[#D0A86C]" /> Live support connection is not configured yet.</p><p className="mt-2 text-xs leading-5 text-white/50">Your request is prepared for a future Chatwoot or live-support integration. We are not claiming an agent is currently online.</p>{details.length > 0 && <dl className="mt-4 grid gap-2 border-t border-white/10 pt-4">{details.map(([label, value]) => <div key={label} className="flex justify-between gap-4 text-xs"><dt className="text-white/45">{label}</dt><dd className="text-right font-semibold text-white/80">{value}</dd></div>)}</dl>}</div><div className="mt-4 flex flex-wrap gap-2"><Link href="/help-center" className="inline-flex items-center gap-2 rounded-[5px] bg-[#D0A86C] px-4 py-2.5 text-xs font-bold text-[#071F1B]">Open Help Center <ArrowUpRight className="size-3.5" /></Link><span className="inline-flex items-center gap-2 px-2 text-[11px] text-white/45"><ShieldCheck className="size-3.5" /> Details remain in this chat</span></div></article>;
}
